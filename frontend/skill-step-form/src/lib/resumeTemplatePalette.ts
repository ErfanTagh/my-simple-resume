/**
 * Shared resume palette — accent blue matches LaTeX Vorlage (`linkColor` / section highlights).
 * All templates should use these defaults unless `styling` overrides them.
 */
export const RESUME_ACCENT_BLUE = "#2563eb";
/** Gray-900 — primary name / title rail (LaTeX `titleColor`) */
export const RESUME_TITLE_GRAY = "#111827";
/** Gray-700 — body copy (LaTeX `textColor`) */
export const RESUME_BODY_GRAY = "#374151";
